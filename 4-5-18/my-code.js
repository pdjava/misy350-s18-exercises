let blist = ["Travel", "End of the semseter", "Finish the semseter strong"] ;
console.log(blist.length);
// "push" adding extra parameters in your arrray
blist.push("Work");
console.log(blist.length);
// "pop" kicks out the list in your array

/*
blist.pop();
blist.pop();
console.log(blist.length);
console.log(blist);
*/

// for loop //

for(let i = 0; i<blist.length; i++) {
  console.log(blist[i]);
}
